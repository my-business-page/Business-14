const loader=document.getElementById('loader');window.addEventListener('load',()=>setTimeout(()=>loader?.classList.add('done'),450));setTimeout(()=>loader?.classList.add('done'),1800);
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu?.addEventListener('click',()=>nav?.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));
const reveal=()=>document.querySelectorAll('.reveal').forEach(el=>{if(el.getBoundingClientRect().top<innerHeight*.88)el.classList.add('visible')});addEventListener('scroll',reveal,{passive:true});addEventListener('load',reveal);setTimeout(reveal,600);
const art=document.querySelector('.art-frame');addEventListener('pointermove',e=>{if(!art)return;const r=art.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;art.style.transform=`perspective(900px) rotateY(${x*4}deg) rotateX(${y*-4}deg)`});addEventListener('pointerleave',()=>{if(art)art.style.transform=''});
